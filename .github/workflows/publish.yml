name: Publish Release

on:
  push:
    branches:
      - 'fix-release'
#  release:
#    types: [ published ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0

      - name: Set up JDK 1.8
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: 8.0.x

      - name: Set up GPG
        run: echo -n "${GPG_PRIVATE_KEY}" | base64 --decode > ${GITHUB_WORKSPACE}/${GPG_KEY_ID}.gpg
        env:
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}

      - name: Set up Maven
        uses: s4u/maven-settings-action@v2.4.1
        with:
          servers: '[{"id": "ossrh", "username": "${{secrets.OSSRH_USERNAME}}", "password": "${{secrets.OSSRH_PASSWORD}}"}]'

      - name: "Maven Build"
        run: ./mvnw -Dinvoker.skip=true install

      - name: "Maven Publish"
        run: |
          ./mvnw -Dinvoker.skip=true package gpg:sign deploy \
                 -Dgpg.keyname=${GPG_KEY_ID} \
                 -Dgpg.passphrase=${GPG_PASSPHRASE} \
                 -Dgpg.secretKeyrin=${GITHUB_WORKSPACE}/${GPG_KEY_ID}.gpg
        env:
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
